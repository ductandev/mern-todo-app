# === Dependencies ===
node_modules

# === Environment / secrets (KHÔNG BAO GIỜ commit) ===
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
/backend/.env
/frontend/.env
ansible/hosts.ini
ansible/ssh-key-digital-ocean
*.pem

# === Terraform ===
# Binary terraform do bash.sh tải về (không phải thư mục terraform/)
/terraform/terraform
terraform_*.zip
terraform.tfvars
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Alertmanager config thật do Ansible render (chứa SMTP password)
monitoring/alertmanager/alertmanager.yml

# === Build output ===
/frontend/build
/coverage

# === Misc ===
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
LICENSE.txt
.wget-hsts
bash_backup*
/frontend/yarn.lock
